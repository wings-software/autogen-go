name: Sample Pipeline_1676370036065
stages:
  - name: build
    spec:
      steps:
        - name: echo
          spec:
            run: echo hello world
          type: script
    type: ci
version: 1
